"""Leads Module — Presentation Layer (FastAPI Routers for Public Lead Form & Admin List)."""

from fastapi import APIRouter

router = APIRouter(prefix="/leads", tags=["Leads Capture"])

# Endpoints will be mounted in Phase 2
